document.addEventListener('DOMContentLoaded', function() {
    // Trigger the fadeIn animation for the main element
    const mainElement = document.getElementById('main');
    mainElement.style.animation = 'fadeIn 1s ease-in-out forwards';
});